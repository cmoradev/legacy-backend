apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: softlinker.app
  namespace: legacy
spec:
  commonName: '*.softlinker.app'
  dnsNames:
    - softlinker.app
    - '*.softlinker.app'
  issuerRef:
    kind: Issuer
    name: letsencrypt-softlinker-app
  secretName: softlinker-app-lts
